{
  "name": "@apite/sw6-cart-tests",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "postman-cart": "node_modules/.bin/newman run -r cli,htmlextra ./postman/collection.json -e ./postman/environment.json -g ./postman/globals.json --reporter-htmlextra-export postman-report.html --color=on --bail",
    "postman-ci": "node_modules/.bin/newman run -r cli,htmlextra,junit ./postman/collection.json -e ./postman/environment.json -g ./postman/globals.json --color=on --insecure --no-insecure-file-read"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "<= 10.0.0"
  },
  "dependencies": {
    "newman": "latest",
    "newman-reporter-htmlextra": "latest"
  }
}
